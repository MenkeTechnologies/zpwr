PAGE_TITLE="ALL Zsh Insert Mode (1/3)"
PAGE_CHAPTER="APPENDIX B: COMPLETE KEYBINDING DUMP"
PAGE_CONTENT() {
    local C=$'\033[36m' G=$'\033[32m' Y=$'\033[33m' R=$'\033[31m'
    local M=$'\033[35m' B=$'\033[1m' D=$'\033[2m' N=$'\033[0m'
    print "  ${B}${M}=== ALL ZSH INSERT MODE (1/3) ===${N}"
    print "  ${D}bindkey -M viins '^@' zpwrExpandTerminateSpace${N}"
    print "  ${D}bindkey -M viins '^A' beginning-of-line${N}"
    print "  ${D}bindkey -M viins '^B' zpwrClipboard${N}"
    print "  ${D}bindkey -M viins '^C' self-insert${N}"
    print "  ${D}bindkey -M viins '^D' list-choices${N}"
    print "  ${D}bindkey -M viins '^E' end-of-line${N}"
    print "  ${D}bindkey -M viins '^F^D' zpwrIntoFzf${N}"
    print "  ${D}bindkey -M viins '^F^F' zpwrCdVimFzfFilesearchWidgetAccept${N}"
    print "  ${D}bindkey -M viins '^F^G' zpwrGoogle${N}"
    print "  ${D}bindkey -M viins '^F^H' zpwrLsoffzf${N}"
    print "  ${D}bindkey -M viins '^F^I' zpwrIntoFzfAg${N}"
    print "  ${D}bindkey -M viins '^F^J' zpwrVerbsWidgetAccept${N}"
    print "  ${D}bindkey -M viins '^F^K' zpwrAlternateQuotes${N}"
    print "  ${D}bindkey -M viins '^F^L' list-choices${N}"
    print "  ${D}bindkey -M viins '^F^M' zzcomplete${N}"
    print "  ${D}bindkey -M viins '^F^N' zpwrVerbsWidget${N}"
    print "  ${D}bindkey -M viins '^F^O' zpwrOpen${N}"
    print "  ${D}bindkey -M viins '^F^P' basicSedSub${N}"
    print "  ${D}bindkey -M viins '^F^R' zpwrAsVar${N}"
    print "  ${D}bindkey -M viins '^F^S' zsh-gacp-CheckDiff${N}"
    print "  ${D}bindkey -M viins '^F^V' edit-command-line${N}"
    print "  ${D}bindkey -M viins '^Fj' zpwrVerbsWidgetAccept${N}"
    print "  ${D}bindkey -M viins '^Fn' zpwrVerbsWidget${N}"
    print "  ${D}bindkey -M viins '^G' what-cursor-position${N}"
    print "  ${D}bindkey -M viins '^H' autopair-delete${N}"
    print "  ${D}bindkey -M viins '^I' fzf-completion${N}"
    print "  ${D}bindkey -M viins '^J' accept-line${N}"
    print "  ${D}bindkey -M viins '^K' zsh-learn-Learn${N}"
    print "  ${D}bindkey -M viins '^L' clear-screen${N}"
    print "  ${D}bindkey -M viins '^M' zpwrMagicEnter${N}"
    print "  ${D}bindkey -M viins '^N' down-history${N}"
    print "  ${D}bindkey -M viins '^O' zpwrEOLorNextTabStop${N}"
    print "  ${D}bindkey -M viins '^O^I' zc-logo${N}"
    print "  ${D}bindkey -M viins '^O^Z' zui-demo-various${N}"
    print "  ${D}bindkey -M viins '^P' up-history${N}"
    print "  ${D}bindkey -M viins '^Q' zpwrLastWordDouble${N}"
    print "  ${D}bindkey -M viins '^R' redo${N}"
    print "  ${D}bindkey -M viins '^S' zsh-gacp-NoCheck${N}"
    print "  ${D}bindkey -M viins '^T' fzf-file-widget${N}"
    print "  ${D}bindkey -M viins '^U' zpwrClearLine${N}"
    print "  ${D}bindkey -M viins '^V^@' zpwrVimFzf${N}"
    print "  ${D}bindkey -M viins '^V^F' zpwrFasdFZF${N}"
    print "  ${D}bindkey -M viins '^V^G' fzf-cd-widget${N}"
    print "  ${D}bindkey -M viins '^V^K' zpwrEmacsFzf${N}"
    print "  ${D}bindkey -M viins '^V^L' zpwrZFZF${N}"
    print "  ${D}bindkey -M viins '^V^N' zpwrVimFzfSudo${N}"
    print "  ${D}bindkey -M viins '^V^O' fzf-tab-complete${N}"
    print "  ${D}bindkey -M viins '^V^P' sudo-command-line${N}"
}
