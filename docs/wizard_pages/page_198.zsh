PAGE_TITLE="ALL Zsh Visual Mode (1/2)"
PAGE_CHAPTER="APPENDIX B: COMPLETE KEYBINDING DUMP"
PAGE_CONTENT() {
    local C=$'\033[36m' G=$'\033[32m' Y=$'\033[33m' R=$'\033[31m'
    local M=$'\033[35m' B=$'\033[1m' D=$'\033[2m' N=$'\033[0m'
    print "  ${B}${M}=== ALL ZSH VISUAL MODE (1/2) ===${N}"
    print "  ${D}bindkey -a 'V' visual-line-mode${N}"
    print "  ${D}bindkey -a 'v' visual-mode${N}"
    print "  ${D}bindkey -M visual '^[' deactivate-region${N}"
    print "  ${D}bindkey -M visual '^[OA' up-line${N}"
    print "  ${D}bindkey -M visual '^[OB' down-line${N}"
    print "  ${D}bindkey -M visual '^[[A' up-line${N}"
    print "  ${D}bindkey -M visual '^[[B' down-line${N}"
    print "  ${D}bindkey -M visual '-' vi-up-line-or-history${N}"
    print "  ${D}bindkey -M visual 'U' vi-up-case${N}"
    print "  ${D}bindkey -M visual 'a\\'' select-quoted${N}"
    print "  ${D}bindkey -M visual 'a'' select-quoted${N}"
    print "  ${D}bindkey -M visual 'a(' select-bracketed${N}"
    print "  ${D}bindkey -M visual 'a)' select-bracketed${N}"
    print "  ${D}bindkey -M visual 'a+' select-quoted${N}"
    print "  ${D}bindkey -M visual 'a,' select-quoted${N}"
    print "  ${D}bindkey -M visual 'a-' select-quoted${N}"
    print "  ${D}bindkey -M visual 'a.' select-quoted${N}"
    print "  ${D}bindkey -M visual 'a/' select-quoted${N}"
    print "  ${D}bindkey -M visual 'a:' select-quoted${N}"
    print "  ${D}bindkey -M visual 'a;' select-quoted${N}"
    print "  ${D}bindkey -M visual 'a<' select-bracketed${N}"
    print "  ${D}bindkey -M visual 'a=' select-quoted${N}"
    print "  ${D}bindkey -M visual 'a>' select-bracketed${N}"
    print "  ${D}bindkey -M visual 'a@' select-quoted${N}"
    print "  ${D}bindkey -M visual 'aB' select-bracketed${N}"
    print "  ${D}bindkey -M visual 'aW' select-a-blank-word${N}"
    print "  ${D}bindkey -M visual 'a[' select-bracketed${N}"
    print "  ${D}bindkey -M visual 'a\\\\\\\\' select-quoted${N}"
    print "  ${D}bindkey -M visual 'a]' select-bracketed${N}"
    print "  ${D}bindkey -M visual 'a\\'' select-quoted${N}"
    print "  ${D}bindkey -M visual 'aa' select-a-shell-word${N}"
    print "  ${D}bindkey -M visual 'ab' select-bracketed${N}"
    print "  ${D}bindkey -M visual 'aw' select-a-word${N}"
    print "  ${D}bindkey -M visual 'a{' select-bracketed${N}"
    print "  ${D}bindkey -M visual 'a|' select-quoted${N}"
    print "  ${D}bindkey -M visual 'a}' select-bracketed${N}"
    print "  ${D}bindkey -M visual 'i\\'' select-quoted${N}"
    print "  ${D}bindkey -M visual 'i'' select-quoted${N}"
    print "  ${D}bindkey -M visual 'i(' select-bracketed${N}"
    print "  ${D}bindkey -M visual 'i)' select-bracketed${N}"
    print "  ${D}bindkey -M visual 'i+' select-quoted${N}"
    print "  ${D}bindkey -M visual 'i,' select-quoted${N}"
    print "  ${D}bindkey -M visual 'i-' select-quoted${N}"
    print "  ${D}bindkey -M visual 'i.' select-quoted${N}"
    print "  ${D}bindkey -M visual 'i/' select-quoted${N}"
    print "  ${D}bindkey -M visual 'i:' select-quoted${N}"
    print "  ${D}bindkey -M visual 'i;' select-quoted${N}"
    print "  ${D}bindkey -M visual 'i<' select-bracketed${N}"
}
