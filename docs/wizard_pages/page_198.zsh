PAGE_TITLE="ALL Zsh Insert Mode (2/3)"
PAGE_CHAPTER="APPENDIX B: COMPLETE KEYBINDING DUMP"
PAGE_CONTENT() {
    local C=$'\033[36m' G=$'\033[32m' Y=$'\033[33m' R=$'\033[31m'
    local M=$'\033[35m' B=$'\033[1m' D=$'\033[2m' N=$'\033[0m'
    print "  ${B}${M}=== ALL ZSH INSERT MODE (2/3) ===${N}"
    print "  ${D}bindkey -M viins '^V^R' history-search-multi-word${N}"
    print "  ${D}bindkey -M viins '^V^S' zpwrZFZF${N}"
    print "  ${D}bindkey -M viins '^V^V' zpwrVimAllWidgetAccept${N}"
    print "  ${D}bindkey -M viins '^V^Z' fzf-history-widget${N}"
    print "  ${D}bindkey -M viins '^V ' zpwrVimFzf${N}"
    print "  ${D}bindkey -M viins '^V,' zpwrFzfEnv${N}"
    print "  ${D}bindkey -M viins '^V.' zpwrFzfAllKeybind${N}"
    print "  ${D}bindkey -M viins '^V/.' zpwrLocateFzfEdit${N}"
    print "  ${D}bindkey -M viins '^V//' zpwrLocateFzf${N}"
    print "  ${D}bindkey -M viins '^V;' zpwrFzfSurround${N}"
    print "  ${D}bindkey -M viins '^Vc' zpwrFzfCommits${N}"
    print "  ${D}bindkey -M viins '^Vk' zpwrFzfVimKeybind${N}"
    print "  ${D}bindkey -M viins '^Vl' zpwrZFZF${N}"
    print "  ${D}bindkey -M viins '^Vm' zpwrVimAllWidget${N}"
    print "  ${D}bindkey -M viins '^Vv' zpwrVimAllWidgetAccept${N}"
    print "  ${D}bindkey -M viins '^W' autopair-delete-word${N}"
    print "  ${D}bindkey -M viins '^X^R' _read_comp${N}"
    print "  ${D}bindkey -M viins '^X^X' zpwrBufferXtrace${N}"
    print "  ${D}bindkey -M viins '^X?' _complete_debug${N}"
    print "  ${D}bindkey -M viins '^XC' _correct_filename${N}"
    print "  ${D}bindkey -M viins '^Xa' _expand_alias${N}"
    print "  ${D}bindkey -M viins '^Xc' _correct_word${N}"
    print "  ${D}bindkey -M viins '^Xd' _list_expansions${N}"
    print "  ${D}bindkey -M viins '^Xe' _expand_word${N}"
    print "  ${D}bindkey -M viins '^Xh' _complete_help${N}"
    print "  ${D}bindkey -M viins '^Xm' _most_recent_file${N}"
    print "  ${D}bindkey -M viins '^Xn' _next_tags${N}"
    print "  ${D}bindkey -M viins '^Xt' _complete_tag${N}"
    print "  ${D}bindkey -M viins '^X~' _bash_list-choices${N}"
    print "  ${D}bindkey -M viins '^Y' zpwrChangeQuotes${N}"
    print "  ${D}bindkey -M viins '^Z' undo${N}"
    print "  ${D}bindkey -M viins '^[' vi-cmd-mode${N}"
    print "  ${D}bindkey -M viins '^[^D' capitalize-word${N}"
    print "  ${D}bindkey -M viins '^[^E' zpwrExpandGlobalAliases${N}"
    print "  ${D}bindkey -M viins '^[^F' sub${N}"
    print "  ${D}bindkey -M viins '^[^L' down-case-word${N}"
    print "  ${D}bindkey -M viins '^[^M' self-insert-unmeta${N}"
    print "  ${D}bindkey -M viins '^[^O' zpwrRunner${N}"
    print "  ${D}bindkey -M viins '^[^P' zpwrUp8widget${N}"
    print "  ${D}bindkey -M viins '^[^T' transpose-words${N}"
    print "  ${D}bindkey -M viins '^[^U' up-case-word${N}"
    print "  ${D}bindkey -M viins '^[ ' zpwrSshRegain${N}"
    print "  ${D}bindkey -M viins '^[,' _history-complete-newer${N}"
    print "  ${D}bindkey -M viins '^[/' _history-complete-older${N}"
    print "  ${D}bindkey -M viins '^[OA' history-substring-search-up${N}"
    print "  ${D}bindkey -M viins '^[OB' history-substring-search-down${N}"
    print "  ${D}bindkey -M viins '^[OC' vi-forward-char${N}"
    print "  ${D}bindkey -M viins '^[OD' vi-backward-char${N}"
}
