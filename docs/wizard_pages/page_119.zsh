PAGE_TITLE="ALL Zsh Insert Mode (3/3)"
PAGE_CHAPTER="APPENDIX B: COMPLETE KEYBINDING DUMP"
PAGE_CONTENT() {
    local C=$'\033[36m' G=$'\033[32m' Y=$'\033[33m' R=$'\033[31m'
    local M=$'\033[35m' B=$'\033[1m' D=$'\033[2m' N=$'\033[0m'
    print "  ${B}${M}=== ALL ZSH INSERT MODE (3/3) ===${N}"
    print "  ${D}bindkey -M viins '^[OP' zpwrUp8widget${N}"
    print "  ${D}bindkey -M viins '^[OQ' sub${N}"
    print "  ${D}bindkey -M viins '^[OQ^[OQ' npm_toggle_install_uninstall${N}"
    print "  ${D}bindkey -M viins '^[OR' zpwrGetrcWidget${N}"
    print "  ${D}bindkey -M viins '^[Q' zi-browse-symbol${N}"
    print "  ${D}bindkey -M viins '^[[1;2D' sub${N}"
    print "  ${D}bindkey -M viins '^[[1;5A' zsh-gacp-CheckDiff${N}"
    print "  ${D}bindkey -M viins '^[[1;5B' zpwrUpdater${N}"
    print "  ${D}bindkey -M viins '^[[1;5C' zpwrTutsUpdate${N}"
    print "  ${D}bindkey -M viins '^[[1;5D' dbz${N}"
    print "  ${D}bindkey -M viins '^[[200~' bracketed-paste${N}"
    print "  ${D}bindkey -M viins '^[[5~' zpwrClipboard${N}"
    print "  ${D}bindkey -M viins '^[[A' history-substring-search-up${N}"
    print "  ${D}bindkey -M viins '^[[B' history-substring-search-down${N}"
    print "  ${D}bindkey -M viins '^[[C' vi-forward-char${N}"
    print "  ${D}bindkey -M viins '^[[D' vi-backward-char${N}"
    print "  ${D}bindkey -M viins '^[[Z' zpwrClipboard${N}"
    print "  ${D}bindkey -M viins '^[c' fzf-cd-widget${N}"
    print "  ${D}bindkey -M viins '^[man' man-command-line${N}"
    print "  ${D}bindkey -M viins '^[~' _bash_complete-word${N}"
    print "  ${D}bindkey -R -M viins '^\\\\\\\\'-'^\\^' self-insert${N}"
    print "  ${D}bindkey -M viins '^_' zpwrAsVar${N}"
    print "  ${D}bindkey -M viins ' ' zpwrExpandSupernaturalSpace${N}"
    print "  ${D}bindkey -M viins '.' self-insert${N}"
    print "  ${D}bindkey -M viins '\\'' zpwrInterceptSurround${N}"
    print "  ${D}bindkey -R -M viins '#'-'&' self-insert${N}"
    print "  ${D}bindkey -R -M viins '''-'(' zpwrInterceptSurround${N}"
    print "  ${D}bindkey -M viins ')' autopair-close${N}"
    print "  ${D}bindkey -R -M viins '*'-'-' self-insert${N}"
    print "  ${D}bindkey -M viins '.' zpwrRationalizeDot${N}"
    print "  ${D}bindkey -R -M viins '/'-'Z' self-insert${N}"
    print "  ${D}bindkey -M viins '[' zpwrInterceptSurround${N}"
    print "  ${D}bindkey -M viins '\\\\\\\\' self-insert${N}"
    print "  ${D}bindkey -M viins ']' autopair-close${N}"
    print "  ${D}bindkey -R -M viins '\\^'-'_' self-insert${N}"
    print "  ${D}bindkey -M viins '\\'' zpwrInterceptSurround${N}"
    print "  ${D}bindkey -R -M viins 'a'-'f' self-insert${N}"
    print "  ${D}bindkey -M viins 'fj' vi-cmd-mode${N}"
    print "  ${D}bindkey -R -M viins 'g'-'j' self-insert${N}"
    print "  ${D}bindkey -M viins 'jf' vi-cmd-mode${N}"
    print "  ${D}bindkey -R -M viins 'k'-'z' self-insert${N}"
    print "  ${D}bindkey -M viins '{' zpwrInterceptSurround${N}"
    print "  ${D}bindkey -M viins '|' self-insert${N}"
    print "  ${D}bindkey -M viins '}' autopair-close${N}"
    print "  ${D}bindkey -M viins '~' self-insert${N}"
    print "  ${D}bindkey -M viins '^?' zpwrInterceptDelete${N}"
    print "  ${D}bindkey -R -M viins '\\M-^@'-'\\M-^?' self-insert${N}"
}
