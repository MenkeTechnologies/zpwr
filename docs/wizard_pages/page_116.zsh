PAGE_TITLE="ALL Tmux Copy-Mode-Vi (2/2)"
PAGE_CHAPTER="APPENDIX B: COMPLETE KEYBINDING DUMP"
PAGE_CONTENT() {
    local C=$'\033[36m' G=$'\033[32m' Y=$'\033[33m' R=$'\033[31m'
    local M=$'\033[35m' B=$'\033[1m' D=$'\033[2m' N=$'\033[0m'
    print "  ${B}${M}=== ALL TMUX COPY-MODE-VI (2/2) ===${N}"
    print "  ${D}bind-key  -T copy-mode-vi n  send-keys -X search-again${N}"
    print "  ${D}bind-key  -T copy-mode-vi o  send-keys -X other-end${N}"
    print "  ${D}bind-key  -T copy-mode-vi q  send-keys -X cancel${N}"
    print "  ${D}bind-key  -T copy-mode-vi r  send-keys -X refresh-from-pane${N}"
    print "  ${D}bind-key  -T copy-mode-vi s  send-keys -X copy-pipe-no-clear 'reattach-to-${N}"
    print "  ${D}bind-key  -T copy-mode-vi t  command-prompt -1 -p '(jump to forward)' { se${N}"
    print "  ${D}bind-key  -T copy-mode-vi v  send-keys -X begin-selection${N}"
    print "  ${D}bind-key  -T copy-mode-vi w  send-keys -X next-word${N}"
    print "  ${D}bind-key  -T copy-mode-vi x  send-keys -X copy-pipe-no-clear 'reattach-to-${N}"
    print "  ${D}bind-key  -T copy-mode-vi y  send-keys -X copy-pipe-no-clear 'reattach-to-${N}"
    print "  ${D}bind-key  -T copy-mode-vi z  send-keys -X copy-pipe-no-clear 'reattach-to-${N}"
    print "  ${D}bind-key  -T copy-mode-vi \\{  send-keys -X previous-paragraph${N}"
    print "  ${D}bind-key  -T copy-mode-vi \\}  send-keys -X next-paragraph${N}"
    print "  ${D}bind-key  -T copy-mode-vi WheelUpPane  select-pane \\; send-keys -X -N 5 s${N}"
    print "  ${D}bind-key  -T copy-mode-vi WheelDownPane  select-pane \\; send-keys -X -N 5${N}"
    print "  ${D}bind-key  -T copy-mode-vi DoubleClick1Pane  select-pane \\; send-keys -X s${N}"
    print "  ${D}bind-key  -T copy-mode-vi TripleClick1Pane  select-pane \\; send-keys -X s${N}"
    print "  ${D}bind-key  -T copy-mode-vi BSpace  send-keys -X cursor-left${N}"
    print "  ${D}bind-key  -T copy-mode-vi Home  send-keys -X start-of-line${N}"
    print "  ${D}bind-key  -T copy-mode-vi End  send-keys -X end-of-line${N}"
    print "  ${D}bind-key  -T copy-mode-vi NPage  send-keys -X page-down${N}"
    print "  ${D}bind-key  -T copy-mode-vi PPage  send-keys -X page-up${N}"
    print "  ${D}bind-key  -T copy-mode-vi Up  send-keys -X cursor-up${N}"
    print "  ${D}bind-key  -T copy-mode-vi Down  send-keys -X cursor-down${N}"
    print "  ${D}bind-key  -T copy-mode-vi Left  send-keys -X cursor-left${N}"
    print "  ${D}bind-key  -T copy-mode-vi Right  send-keys -X cursor-right${N}"
    print "  ${D}bind-key  -T copy-mode-vi M-x  send-keys -X jump-to-mark${N}"
    print "  ${D}bind-key  -T copy-mode-vi C-b  send-keys -X page-up${N}"
    print "  ${D}bind-key  -T copy-mode-vi C-c  send-keys -X cancel${N}"
    print "  ${D}bind-key  -T copy-mode-vi C-d  send-keys -X halfpage-down${N}"
    print "  ${D}bind-key  -T copy-mode-vi C-e  send-keys -X scroll-down${N}"
    print "  ${D}bind-key  -T copy-mode-vi C-f  send-keys -X page-down${N}"
    print "  ${D}bind-key  -T copy-mode-vi C-h  send-keys -X -N 4 cursor-left${N}"
    print "  ${D}bind-key  -T copy-mode-vi C-j  send-keys -X -N 4 cursor-down${N}"
    print "  ${D}bind-key  -T copy-mode-vi C-k  send-keys -X -N 4 cursor-up${N}"
    print "  ${D}bind-key  -T copy-mode-vi C-l  send-keys -X -N 4 cursor-right${N}"
    print "  ${D}bind-key  -T copy-mode-vi C-n  send-keys -X page-down${N}"
    print "  ${D}bind-key  -T copy-mode-vi C-p  send-keys -X page-up${N}"
    print "  ${D}bind-key  -T copy-mode-vi C-u  send-keys -X halfpage-up${N}"
    print "  ${D}bind-key  -T copy-mode-vi C-v  send-keys -X rectangle-toggle${N}"
    print "  ${D}bind-key  -T copy-mode-vi C-y  send-keys -X scroll-up${N}"
    print "  ${D}bind-key  -T copy-mode-vi C-Up  send-keys -X scroll-up${N}"
    print "  ${D}bind-key  -T copy-mode-vi C-Down  send-keys -X scroll-down${N}"
}
