PAGE_TITLE="ALL Tmux Copy-Mode-Vi (1/2)"
PAGE_CHAPTER="APPENDIX B: COMPLETE KEYBINDING DUMP"
PAGE_CONTENT() {
    local C=$'\033[36m' G=$'\033[32m' Y=$'\033[33m' R=$'\033[31m'
    local M=$'\033[35m' B=$'\033[1m' D=$'\033[2m' N=$'\033[0m'
    print "  ${B}${M}=== ALL TMUX COPY-MODE-VI (1/2) ===${N}"
    print "  ${D}bind-key  -T copy-mode-vi Enter  send-keys -X copy-pipe-and-cancel 'reatta${N}"
    print "  ${D}bind-key  -T copy-mode-vi Escape  send-keys -X clear-selection${N}"
    print "  ${D}bind-key  -T copy-mode-vi Space  send-keys -X begin-selection${N}"
    print "  ${D}bind-key  -T copy-mode-vi \\#  send-keys -FX search-backward -- '#{copy_cu${N}"
    print "  ${D}bind-key  -T copy-mode-vi \\  send-keys -X end-of-line${N}"
    print "  ${D}bind-key  -T copy-mode-vi \\%  send-keys -X next-matching-bracket${N}"
    print "  ${D}bind-key  -T copy-mode-vi *  send-keys -FX search-forward -- '#{copy_curso${N}"
    print "  ${D}bind-key  -T copy-mode-vi ,  send-keys -X jump-reverse${N}"
    print "  ${D}bind-key  -T copy-mode-vi /  command-prompt -i -p '(search down)' 'send -X${N}"
    print "  ${D}bind-key  -T copy-mode-vi 0  send-keys -X start-of-line${N}"
    print "  ${D}bind-key  -T copy-mode-vi 1  command-prompt -N -I 1 -p (repeat) { send-key${N}"
    print "  ${D}bind-key  -T copy-mode-vi 2  command-prompt -N -I 2 -p (repeat) { send-key${N}"
    print "  ${D}bind-key  -T copy-mode-vi 3  command-prompt -N -I 3 -p (repeat) { send-key${N}"
    print "  ${D}bind-key  -T copy-mode-vi 4  command-prompt -N -I 4 -p (repeat) { send-key${N}"
    print "  ${D}bind-key  -T copy-mode-vi 5  command-prompt -N -I 5 -p (repeat) { send-key${N}"
    print "  ${D}bind-key  -T copy-mode-vi 6  command-prompt -N -I 6 -p (repeat) { send-key${N}"
    print "  ${D}bind-key  -T copy-mode-vi 7  command-prompt -N -I 7 -p (repeat) { send-key${N}"
    print "  ${D}bind-key  -T copy-mode-vi 8  command-prompt -N -I 8 -p (repeat) { send-key${N}"
    print "  ${D}bind-key  -T copy-mode-vi 9  command-prompt -N -I 9 -p (repeat) { send-key${N}"
    print "  ${D}bind-key  -T copy-mode-vi :  command-prompt -p '(goto line)' { send-keys -${N}"
    print "  ${D}bind-key  -T copy-mode-vi \\;  send-keys -X jump-again${N}"
    print "  ${D}bind-key  -T copy-mode-vi ?  command-prompt -i -p '(search up)' 'send -X s${N}"
    print "  ${D}bind-key  -T copy-mode-vi A  send-keys -X append-selection-and-cancel${N}"
    print "  ${D}bind-key  -T copy-mode-vi B  send-keys -X previous-space${N}"
    print "  ${D}bind-key  -T copy-mode-vi D  send-keys -X copy-pipe-end-of-line-and-cancel${N}"
    print "  ${D}bind-key  -T copy-mode-vi E  send-keys -X next-space-end${N}"
    print "  ${D}bind-key  -T copy-mode-vi F  command-prompt -1 -p '(jump backward)' { send${N}"
    print "  ${D}bind-key  -T copy-mode-vi G  send-keys -X history-bottom${N}"
    print "  ${D}bind-key  -T copy-mode-vi H  send-keys -X top-line${N}"
    print "  ${D}bind-key  -T copy-mode-vi J  send-keys -X scroll-down${N}"
    print "  ${D}bind-key  -T copy-mode-vi K  send-keys -X scroll-up${N}"
    print "  ${D}bind-key  -T copy-mode-vi L  send-keys -X bottom-line${N}"
    print "  ${D}bind-key  -T copy-mode-vi M  send-keys -X middle-line${N}"
    print "  ${D}bind-key  -T copy-mode-vi N  send-keys -X search-reverse${N}"
    print "  ${D}bind-key  -T copy-mode-vi P  send-keys -X toggle-position${N}"
    print "  ${D}bind-key  -T copy-mode-vi T  command-prompt -1 -p '(jump to backward)' { s${N}"
    print "  ${D}bind-key  -T copy-mode-vi V  send-keys -X select-line${N}"
    print "  ${D}bind-key  -T copy-mode-vi W  send-keys -X next-space${N}"
    print "  ${D}bind-key  -T copy-mode-vi X  send-keys -X set-mark${N}"
    print "  ${D}bind-key  -T copy-mode-vi ^  send-keys -X back-to-indentation${N}"
    print "  ${D}bind-key  -T copy-mode-vi b  send-keys -X previous-word${N}"
    print "  ${D}bind-key  -T copy-mode-vi e  send-keys -X next-word-end${N}"
    print "  ${D}bind-key  -T copy-mode-vi f  command-prompt -1 -p '(jump forward)' { send-${N}"
    print "  ${D}bind-key  -T copy-mode-vi g  send-keys -X history-top${N}"
    print "  ${D}bind-key  -T copy-mode-vi h  send-keys -X cursor-left${N}"
    print "  ${D}bind-key  -T copy-mode-vi j  send-keys -X cursor-down${N}"
    print "  ${D}bind-key  -T copy-mode-vi k  send-keys -X cursor-up${N}"
    print "  ${D}bind-key  -T copy-mode-vi l  send-keys -X cursor-right${N}"
}
