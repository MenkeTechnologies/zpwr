# -*- mode: sh -*-
# vim: set ft=sh:
function zpwrEmacsPluginList(){

    command ls -d \
        "$HOME/.emacs.d/.cache/quelpa/build/"*(/N) \
        "$HOME/.emacs.d/elpa/"*(/N) 2>/dev/null
}

zpwrEmacsPluginList "$@"
