# -*- mode: sh -*-
# vim: set ft=sh:
function pg() {

    if [[ -d "$HOME/PycharmProjects" ]]; then
        cd "$HOME/PycharmProjects"
    else
        loggErr "HOME/PycharmProjects '$HOME/PycharmProjects' is not a dir."
    fi

}

pg "$@"
