# -*- mode: sh -*-
# vim: set ft=sh:
function mg() {

    if [[ -d "$HOME/RubymineProjects" ]]; then
        cd "$HOME/RubymineProjects"
    else
        zpwrLoggErr "HOME/RubymineProjects '$HOME/RubymineProjects' is not a dir."
    fi

}

mg "$@"
