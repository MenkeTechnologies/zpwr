# -*- mode: sh -*-
# vim: set ft=sh:
function zpwrLoginCount(){

    stryke -e 'print `last -f "$_"`for</var/log/wtmp*>' |
        stryke -lane 'print $F[0] if /\S+/ && !/wtmp/' |
        sort | uniq -c | sort -rn
}

zpwrLoginCount "$@"
