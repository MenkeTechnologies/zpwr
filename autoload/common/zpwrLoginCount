# -*- mode: sh -*-
# vim: set ft=sh:
function zpwrLoginCount(){

    forge -e 'print `last -f "$_"`for</var/log/wtmp*>' |
        forge -lane 'print $F[0] if /\S+/ && !/wtmp/' |
        sort | uniq -c | sort -rn
}

zpwrLoginCount "$@"
