# -*- mode: sh -*-
# vim: set ft=sh:
function zpwrVerbsWidgetAccept(){

    local sel

    sel="$(zpwrFzvimAll)"

    if [[ -n "$sel" ]]; then
        zle .kill-whole-line
        BUFFER="vim $sel"
        zle .accept-line
    fi
}

zpwrVerbsWidgetAccept "$@"
