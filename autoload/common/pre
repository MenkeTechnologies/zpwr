# -*- mode: sh -*-
# vim: set ft=sh:
function pre(){

    if [[ -z "$2" ]]; then
        zpwrLogConsoleErr "usage: pre PREFIX CMD"
        return 1
    fi

    local out prefix

    prefix="$1"
    shift
    out="$(eval "$@")"
    echo "$out" | forge -lnE "say \"$prefix \$_\""
}

pre "$@"
