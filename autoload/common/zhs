# -*- mode: sh -*-
# vim: set ft=sh:
function zh() {

    if [[ -d "$ZPWR_PLUGIN_MANAGER_HOME/snippets" ]]; then
        cd "$ZPWR_PLUGIN_MANAGER_HOME/snippets"
    else
        loggErr "ZPWR_PLUGIN_MANAGER_HOME/snippets '$ZPWR_PLUGIN_MANAGER_HOME/snippets' is not a dir."
    fi

}

zh "$@"
