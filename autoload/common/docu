# -*- mode: sh -*-
# vim: set ft=sh:
function docu() {

    if [[ -d "$ZPWR_DOC" ]]; then
        cd "$ZPWR_DOC"
    else
        zpwrLoggErr "ZPWR_DOC '$ZPWR_DOC' is not a dir."
    fi

}

docu "$@"
