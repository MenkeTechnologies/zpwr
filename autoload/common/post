# -*- mode: sh -*-
# vim: set ft=sh:
function post(){

    if [[ -z "$2" ]]; then
        zpwrLogConsoleErr "usage: post SUFFIX CMD"
        return 1
    fi

    local out postfix
    postfix="$1"
    shift
    out="$(eval "$@")"
    echo "$out" | forge -lnE "say \"\$_ $postfix\""
}

post "$@"
