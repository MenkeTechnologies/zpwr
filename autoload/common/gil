# -*- mode: sh -*-
# vim: set ft=sh:
function gil(){

    if ! isGitDir; then
       loggNotGit
       return 1
    fi

    local file

    file=".git/info/exclude"

    if [[ ! -f "$file" ]]; then
       zpwrLoggErr "$file must exist"
       return 1
    fi

    ${=EDITOR} "$file"

}

gil "$@"
