# -*- mode: sh -*-
# vim: set ft=sh:
function ___zpwrEnvVars(){

        env |
            command perl -ne 'print "$1\n" if m{^('$ZPWR_REPO_NAME'[A-Z0-9_]+)=}i' |
        eval "$ZPWR_FZF -m --border $FZF_ENV_OPTS" | perl -pe 's@\n@ @g'
}

___zpwrEnvVars "$@"
