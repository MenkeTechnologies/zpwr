# -*- mode: sh -*-
# vim: set ft=sh:
# no longer needed: p10k prompt_dir uses %~ natively without auto_name_dirs
# explicit hash -d entries in zpwrBindDirs resolve correctly via %~
function zpwrOverridePromptDir(){
    :
}

zpwrOverridePromptDir "$@"
