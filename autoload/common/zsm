# -*- mode: sh -*-
# vim: set ft=sh:
function zsm() {

    if [[ -d "$ZPWR_SCRIPTS_MAC" ]]; then
        cd "$ZPWR_SCRIPTS_MAC"
    else
        loggErr "ZPWR_SCRIPTS_MAC '$ZPWR_SCRIPTS_MAC' is not a dir."
    fi

}

zsm "$@"
