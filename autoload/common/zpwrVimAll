# -*- mode: sh -*-
# vim: set ft=sh:
function zpwrVimAll(){

    builtin cd "$ZPWR" || {
        zpwrLogConsoleErr "cannot cd to $ZPWR"
        return 1
    }
    ${=ZPWR_VIM} "${=$(zpwrListFiles)}"
    zpwrClearList
    zpwrIsGitDir && git diff HEAD
}

zpwrVimAll "$@"
