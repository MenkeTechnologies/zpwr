# -*- mode: sh -*-
# vim: set ft=sh:
function asg() {

    if [[ -d "$HOME/AndroidStudioProjects" ]]; then
        zpwrCd "$HOME/AndroidStudioProjects"
    else
        zpwrLogConsoleErr "HOME/AndroidStudioProjects '$HOME/AndroidStudioProjects' is not a dir."
    fi

}

asg "$@"
