# -*- mode: sh -*-
# vim: set ft=sh:
function ig() {

    if [[ -d "$HOME/IdeaProjects" ]]; then
        cd "$HOME/IdeaProjects"
    else
        zpwrLoggErr "HOME/IdeaProjects '$HOME/IdeaProjects' is not a dir."
    fi

}

ig "$@"
