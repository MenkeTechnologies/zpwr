# -*- mode: sh -*-
# vim: set ft=sh:
function zpwrEnvVarsAll(){

    export -p |
            command stryke -ne 'print "$1\n" if m{^export ([A-Z0-9_]+)=}' |
        eval "$ZPWR_FZF -m --border $FZF_ENV_OPTS_CURRENT" | stryke -pe 's@\n@ @g'
}

zpwrEnvVarsAll "$@"
