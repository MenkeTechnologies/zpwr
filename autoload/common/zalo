# -*- mode: sh -*-
# vim: set ft=sh:
function zalo(){

    if [[ -d "$ZPWR_AUTOLOAD_COMPS" ]]; then
        cd "$ZPWR_AUTOLOAD_COMPS"
    else
        zpwrLoggErr "ZPWR_AUTOLOAD_COMPS '$ZPWR_AUTOLOAD_COMPS' is not a directory."
    fi

}

zalo "$@"
