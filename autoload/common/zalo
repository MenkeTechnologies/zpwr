# -*- mode: sh -*-
# vim: set ft=sh:
function zalo(){

    if [[ -d "$ZPWR_AUTOLOAD_COMPS" ]]; then
        zpwrCd "$ZPWR_AUTOLOAD_COMPS"
    else
        zpwrLogConsoleErr "ZPWR_AUTOLOAD_COMPS '$ZPWR_AUTOLOAD_COMPS' is not a directory."
    fi

}

zalo "$@"
