# -*- mode: sh -*-
# vim: set ft=sh:
function zte() {

    if [[ -d "$ZPWR_TEST" ]]; then
        zpwrCd "$ZPWR_TEST"
    else
        zpwrLogConsoleErr "ZPWR_TEST '$ZPWR_TEST' is not a dir."
    fi

}

zte "$@"
