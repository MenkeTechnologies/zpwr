# -*- mode: sh -*-
# vim: set ft=sh:
function zhb() {

    if [[ -d "$ZPWR_PLUGIN_MANAGER_HOME/bin" ]]; then
        zpwrCd "$ZPWR_PLUGIN_MANAGER_HOME/bin"
    else
        zpwrLogConsoleErr "ZPWR_PLUGIN_MANAGER_HOME/bin '$ZPWR_PLUGIN_MANAGER_HOME/bin' is not a dir."
    fi

}

zhb "$@"
