# -*- mode: sh -*-
# vim: set ft=sh:
function zhb() {

    if [[ -d "$ZPWR_PLUGIN_MANAGER_HOME/bin" ]]; then
        cd "$ZPWR_PLUGIN_MANAGER_HOME/bin"
    else
        zpwrLoggErr "ZPWR_PLUGIN_MANAGER_HOME/bin '$ZPWR_PLUGIN_MANAGER_HOME/bin' is not a dir."
    fi

}

zhb "$@"
